\documentclass{article}
\usepackage[margin=1in]{geometry}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{booktabs}
\usepackage{placeins}
\usepackage[hypertexnames=false]{hyperref}
\renewcommand{\figurename}{Fig.}

\graphicspath{{results/}{v1_v2_comparison/}{plots/}}

\title{Calibration results:\\
MCMC inference of subgrid + cosmology parameters}
\author{}
\date{}

\begin{document}
\maketitle

\section{Setup}

The likelihood combines emulator predictions with published
observations, summed over the requested observables:
\begin{equation}
\ln\mathcal{L}(\theta) = -\tfrac{1}{2}\sum_{o\in \mathrm{obs}}
\sum_{i} \frac{(y^{\rm obs}_{o,i} - \mu^{\rm emu}_o(\theta;x_i))^2}
              {\sigma^{\rm obs\,2}_{o,i}}.
\end{equation}
Sampling uses \texttt{emcee} with 400 walkers, 100 burn-in steps, 4000
production steps and parallel evaluation. Priors are Gaussian, centred
at the design midpoint with $\sigma=$ half the design range, except
$\epsilon_{\rm kin}$ (index 4), which has a flat prior. Walkers are
initialised uniformly across each parameter's design range.

\paragraph{Project fiducial cosmology.} When cosmology is fixed
(\texttt{param\_mode: subgrid}), the values used are
$\omega_{\rm m}\!\equiv\!\Omega_m h^2=0.14176$ and $\sigma_8=0.8102$
(from $\Omega_{\rm cdm}=0.26067$, $\Omega_b h^2=0.02242$, $H_0=67.66$,
$n_s=0.9665$).

\paragraph{Trials.} Configurations live in
\texttt{Inference/configs/} and are deep-merged on top of
\texttt{\_defaults.yaml}. Headline trials:
\begin{itemize}
  \item \texttt{GSMF\_5p\_fid\_cosmo}: GSMF only, cosmology fixed, 5
        subgrid parameters free.
  \item \texttt{GSMF\_7p}: GSMF only, all 7 parameters free.
  \item \texttt{GSMF\_CGD\_fGas\_7p}: three-observable joint
        calibration, all 7 parameters free.
  \item \texttt{CGD\_CGED\_cluster}: cluster-only fit, cosmology fixed.
  \item \texttt{sigma8\_vkin\_custom}: targeted custom-parameter fit
        ($v_{\rm kin}, \epsilon_{\rm kin}, \sigma_8$).
\end{itemize}

\FloatBarrier

\section{GSMF-only headline fits}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=1\linewidth]{plot_GSMF_5p_mean.png}
    \caption{\texttt{GSMF\_5p\_fid\_cosmo}: triangle plot of the
    five-parameter (subgrid only) posterior obtained from the
    GSMF emulator at the project fiducial cosmology, alongside the
    emulator best-fit overlay against Driver+2022 (right column).
    All four \emph{shared-range} subgrid parameters
    ($\kappa_{\rm w}, e_{\rm w}, v_{\rm kin}, \epsilon_{\rm kin}$)
    are well constrained; $M_{\rm seed}$ is the least constrained.}
    \label{fig:gsmf_5p}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=1\linewidth]{plot_GSMF_7p_mean.png}
    \caption{\texttt{GSMF\_7p}: same as above but with cosmology free.
    The GSMF alone constrains $\sigma_8$ tightly through the
    high-mass tail but leaves $\omega_{\rm m}$ poorly constrained at
    this resolution. Subgrid posteriors broaden when cosmology is
    free, as expected from the additional $\sigma_8$--$\kappa_{\rm w}$
    and $\sigma_8$--$M_{\rm seed}$ degeneracies.}
    \label{fig:gsmf_7p}
\end{figure}

\FloatBarrier

\section{v1 (Flamingo) $\leftrightarrow$ v2 (CosmoHydro) GSMF comparison}

The new (v2) pipeline is benchmarked against the legacy v1
(Flamingo/Clean) inference on a GSMF-only fit. Both pipelines fit the
same Driver+2022 GSMF with mathematically identical priors and
likelihood; the difference is the simulation campaign underneath
(v1: 128\,Mpc HACC, 64 sims, 5p, fixed cosmology;
 v2: 400\,Mpc HAvoCC, 110-sim 7p design with cosmology varied and 5
sims held out for validation).

\begin{figure}[htbp]
    \centering
    \includegraphics[width=1\linewidth]{corner_overlay.png}
    \caption{Triangle overlay of v1 (gray, 5p), v2 5p
    \texttt{GSMF\_5p\_fid\_cosmo} (blue, cosmology fixed at the project
    fiducial), and v2 7p \texttt{GSMF\_7p} (red, cosmology free). The
    shared subgrid axes are shown. Contours show $1\sigma$ and
    $2\sigma$ filled. The two v2 fits agree on the four shared-range
    subgrid parameters; both show a small but consistent shift relative
    to v1 toward weaker AGN feedback, attributed to the different
    simulation campaign rather than to a pipeline change.}
    \label{fig:corner_overlay}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.85\linewidth]{summary_stats_compare.png}
    \caption{Posterior-median emulator predictions overlaid on
    observations for GSMF, $f_{\rm gas}$, and CGD. Bands show the 16/84
    percentile spread from 200 posterior samples. All three chains
    reproduce the observed GSMF amplitude within errors. CGD
    predictions diverge at small radius: the v1 emulator (gray,
    trained on a different sim suite) predicts a steeper inner core
    than the v2 emulators, illustrating that the cluster-gas profile
    discriminates between simulation campaigns even when the GSMF
    constraint is identical.}
    \label{fig:summary_stats_compare}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.85\linewidth]{gsmf_compare.png}
    \caption{Zoom on the GSMF posterior predictive: the three chains
    are statistically indistinguishable on the GSMF datapoints, even
    though they reach different best-fit subgrid parameters. This
    confirms that the GSMF alone is degenerate with respect to the
    feedback parameters that distinguish v1 from v2.}
    \label{fig:gsmf_compare}
\end{figure}

\paragraph{Posterior medians.} For reference (scaled units):

\begin{center}
\begin{tabular}{lccc}
\toprule
Parameter           & v1     & v2 5p   & v2 7p \\
\midrule
$\kappa_{\rm w}$    & 3.144  & 2.920   & 2.844 \\
$e_{\rm w}$         & 0.495  & 0.522   & 0.489 \\
$M_{\rm seed}/10^6$ & 0.779  & 1.109   & 1.073 \\
$v_{\rm kin}/10^4$  & 0.682  & 0.646   & 0.648 \\
$\epsilon_{\rm kin}/10^1$ & 0.617 & 0.532 & 0.644 \\
$\omega_{\rm m}$    & --     & --      & 0.1411 \\
$\sigma_8$          & --     & --      & 0.7787 \\
\bottomrule
\end{tabular}
\end{center}

The v2 7p $\omega_{\rm m}=0.141$ and $\sigma_8=0.779$ are consistent
with the project fiducial used by the 5p fit ($\omega_{\rm m}=0.142$,
$\sigma_8=0.810$), validating the choice of cosmology fix for
subgrid-only calibrations.

\FloatBarrier

\section{Outlook}

\begin{itemize}
  \item Multi-$z$ GSMF MCMC, exploiting the redshift-aware emulators
        from training notebook 02. The likelihood scaffolding already
        supports per-snapshot likelihoods; the trial config is the
        next planned addition.
  \item Cluster-profile-only calibration
        (\texttt{CGD\_CGED\_cluster}) with cosmology fixed at the
        project fiducial, to isolate the subgrid information in
        cluster-scale baryonic observables.
  \item Joint $P(k)$ + cluster constraints: with the suppression
        emulator validated, the $P(k)$ likelihood can be combined with
        $f_{\rm gas}$ for an independent baryonic-feedback handle.
\end{itemize}

\end{document}
