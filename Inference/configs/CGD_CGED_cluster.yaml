# Trial: Cluster-only statistics (CGD + CGED), subgrid parameters only.
# Shared data/mcmc/obs_dirs/output_dir come from _defaults.yaml.
trial_name: CGD_CGED_cluster

observables:
  - CGD
  - CGED

param_mode: subgrid

# Cosmology fixed at the project fiducial.
fixed_params:
  omega_m: 0.14176
  sigma_8: 0.8102
