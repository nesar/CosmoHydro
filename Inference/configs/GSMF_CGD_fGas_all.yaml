# Trial: GSMF + CGD + fGas, all 7 parameters (subgrid + cosmo).
# Like GSMF_CGD_fGas_7p but with eps_kin Gaussian (no flat-prior override).
# Shared data/mcmc/obs_dirs/output_dir come from _defaults.yaml.
trial_name: GSMF_CGD_fGas_all

observables:
  - GSMF
  - CGD
  - fGas

param_mode: subgrid+cosmo
